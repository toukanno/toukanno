# AGENTS.md — 静的HTML リポジトリ用テンプレート

## repo-auditor

**役割**: リポジトリの健全性を確認し、問題点を棚卸しする

- **入力**: リポジトリのルートディレクトリ
- **出力**: 状態レポート（Git状態、ファイル構成、リンク切れ、画像欠損）
- **やってよいこと**:
  - `git status`, `git log`, `git branch` による状態確認
  - HTML / CSS / JS ファイルの構成確認
  - 外部リンク・画像パスの整合性チェック
  - `gh issue list` による未解決Issue確認
- **やってはいけないこと**:
  - ファイルの変更・削除
  - コミット・プッシュ
- **優先順位**: 最初に実行
- **引き継ぎ**: 問題点リストを fixer / doc-writer に渡す

---

## fixer

**役割**: 軽微なHTML/CSS/JS修正を行う

- **入力**: repo-auditor の問題点リスト、または明示的な修正指示
- **出力**: 修正済みファイル、commit、PR
- **やってよいこと**:
  - HTML構文修正（閉じタグ漏れ、属性修正）
  - CSS修正（レスポンシブ対応、表示崩れ）
  - JS軽微修正（console.log削除、未使用変数）
  - meta タグ・OGP修正
  - アクセシビリティ改善（alt属性追加等）
- **やってはいけないこと**:
  - デザインの大幅変更
  - 新機能の追加
  - main への直接push（必ずPR経由）
- **優先順位**: repo-auditor の後
- **引き継ぎ**: 修正後に tester へ

---

## tester

**役割**: 表示確認・リンクチェック・バリデーションを行う

- **入力**: リポジトリのルートディレクトリ
- **出力**: チェック結果レポート
- **やってよいこと**:
  - HTMLバリデーション（W3C準拠チェック）
  - リンク切れ検出
  - 画像パス確認
  - `index.html` の存在確認
  - ブラウザで開けることの確認（`python3 -m http.server`）
- **やってはいけないこと**:
  - ファイルの変更
- **優先順位**: fixer の後
- **引き継ぎ**: 問題があれば fixer に差し戻し

---

## doc-writer

**役割**: ドキュメントの補完・整備を行う

- **入力**: リポジトリの現状、repo-auditor の結果
- **出力**: 更新された README
- **やってよいこと**:
  - README のセクション追加・改善
  - デモURL・スクリーンショットの記載
  - 技術スタック・構成の記載
- **やってはいけないこと**:
  - コードの変更
  - 既存ドキュメントの大幅書き換え
- **優先順位**: 最後に実行
- **引き継ぎ**: なし

---

## 実行フロー

```
repo-auditor → fixer → tester → doc-writer
     ↑                    |
     └────── 失敗時 ──────┘
```
